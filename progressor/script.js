$(document).ready(function () {    $(".startTimer").on("click", function (e) {        var par = $(this).parents("[data-for='barWrapper']"),            initTim = par.find('[data-timer="timer"]').text(),            min = parseInt(initTim.split(":")[0]),            sec = parseInt(initTim.split(":")[1]),            time = ((min * 60) + sec) * 1000;        par.find('[data-cover="progresser"]').animate({'width': '100%'}, time, 'linear');        var timer = setInterval(function () {            var newMin, newSec, newtime;            if(sec == 0) {                newSec = sec = 59;                newMin = --min            }            else if(sec<=59 && sec>=10) {                newSec = --sec;            }            else if(sec < 10) {                newSec = "0" + --sec;            }            if(min<10) {                newMin = "0" + min;            }            else {                newMin = min;            }            newTime = newMin + ":" + newSec;            par.find('[data-timer="timer"]').text(newTime);            if(par.find('[data-timer="timer"]').text() == "00:00") {                clearInterval(timer);            }        }, 1000)    });})