$(function() {    var a, b = 0;    $(".startTimer").on("click", function() {        var par = $(this).parents('[data-for="barWrapper"]');        timerBar(par)    }),     $(".play").on("click", function(a) {        var par = $(this).parents('[data-for="barWrapper"]');        0 == b && (startTime = par.find('[data-timer="timer"]').text(), b = 1);         timerBar(par);    }),     $(".pause").on("click", function(a) {        var par = $(this).parents('[data-for="barWrapper"]');        par.find(".pause").attr("disabled", "disabled").addClass("disabled");        pauseBar(par);    }),     $(".stop").on("click", function(a) {        var par = $(this).parents('[data-for="barWrapper"]');        par.find(".pause,.stop").attr("disabled", "disabled").addClass("disabled");        par.find(".play").removeAttr("disabled").removeClass("disabled");        stopBar(par);    }),     $(".increaseBar").on("click", function(a) {        var par = $(this).parents('[data-for="barWrapper"]'),            percentage = parseInt(par.find('[data-progress="progressPercent"]').text().split("%")[0]),            value = parseInt($(this).val().split(" ")[1]);        if (percentage < 100 && (100 - percentage >= value)) {            var width = parseInt(percentage) + value + "%";            par.find('[data-cover="progresser"]').css({width: width});            par.find('[data-progress="progressPercent"]').text(width);             setColor(par, width);        }    }),     $(".reduceBar").on("click", function(a) {        var par = $(this).parents('[data-for="barWrapper"]'),            percentage = parseInt(par.find('[data-progress="progressPercent"]').text().split("%")[0]),            value = parseInt($(this).val().split(" ")[1]);        if (percentage >= value) {            var width = parseInt(percentage) - value + "%";            par.find('[data-cover="progresser"]').css({width: width});            par.find('[data-progress="progressPercent"]').text(width),             setColor(par, width);        }    }),     setColor = function(par, width) {        var progresser = par.find('[data-cover="progresser"]');        ("100%" == width) ? progresser.css({"background-color": "red"}) : progresser.css({"background-color": "#16a183"});    },     timerBar = function(par) {        par.find(".play").attr("disabled", "disabled").addClass("disabled");         par.find(".pause,.stop").removeAttr("disabled").removeClass("disabled");        var c, d, e;        if (a = setInterval(function() {                if (c = par.find('[data-timer="timer"]').text(), d = c.split(":")[0], e = c.split(":")[1], barwidth = parseInt(par.find('[data-bar="progressiveBar"]').outerWidth()), e > 0 ? (e = e > 10 ? parseInt(e - 1) : "0" + parseInt(e - 1), c = d + ":" + e) : 0 == e && d > 0 ? (d = d > 10 ? parseInt(d - 1) : "0" + parseInt(d - 1), c = d + ":59") : (c = "00:00", par.find('[data-cover="progresser"]').css({                        "background-color": "red"                    }),                     par.find(".pause").attr("disabled", "disabled").addClass("disabled"), clearInterval(a)),                     par.find('[data-timer="timer"]').text(c),                     par.find('[data-progress="progressPercent"]')) {                    var f = parseInt(100 * par.find('[data-cover="progresser"]').width() / barwidth) + "%";                    par.find('[data-progress="progressPercent"]').text(f)                }            }, 1000);             par.find('[data-timer="timer"]')) {                var g = par.find('[data-timer="timer"]').text().split(":");                    h = 1000 * (60 * parseInt(g[0]) + parseInt(g[1]));                                par.find('[data-cover="progresser"]').animate({                    width: "100%"                }, h, "linear")        }    },     pauseBar = function(par) {        clearInterval(a);         par.find('[data-cover="progresser"]').stop();        par.find(".play").removeAttr("disabled").removeClass("disabled");    },     stopBar = function(par) {        clearInterval(a);         par.find('[data-cover="progresser"]').stop().css({width: "0px"});         par.find('[data-timer="timer"]').text(startTime);         par.find('[data-progress="progressPercent"]').text("0%");    }});